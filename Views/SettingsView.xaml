<UserControl
    x:Class="CodeConcussion.KVL.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    Background="WhiteSmoke"
    Width="200"
    Height="100">

    <StackPanel Margin="4">
        <Button
            x:Name="Close"
            HorizontalAlignment="Right"
            Margin="4"
            DockPanel.Dock="Top">
            <Image Source="../Resources/Close.png" Width="16" Height="16" />
        </Button>

        <StackPanel Orientation="Horizontal">
            <Label Content="_Background" Target="{Binding ElementName=BackgroundColor}" />
            <xctk:ColorPicker
                x:Name="BackgroundColor"
                SelectedColor="{Binding BackgroundColor}"
                Width="110"
                Margin="4" />
        </StackPanel>

        <CheckBox x:Name="PlayErrorSound" Content="_Error sound" Margin="4 8" />
    </StackPanel>
</UserControl>